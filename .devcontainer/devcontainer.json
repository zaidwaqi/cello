{
    // image being used
     "image": "quay.io/centos/centos:stream8",
    // set minimum cpu
     "hostRequirements": {
         "cpus": 4
     },
    "updateContentCommand": "dnf install gcc rpm-build rpm-devel rpmdevtools tree"
}
