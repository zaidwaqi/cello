{
    // image being used
     "image": "quay.io/centos/centos:stream8",
    // set minimum cpu
     "hostRequirements": {
         "cpus": 4
     },
    "updateContentCommand": "dnf group install -y 'Development Tools' gcc rpm-build rpm-devel rpmdevtools tree"
}
